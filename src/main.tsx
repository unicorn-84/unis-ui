import React from 'react';
import ReactDOM from 'react-dom';
import { TempButton } from '.';

ReactDOM.render(
  <React.StrictMode>
    <TempButton aria-label="Share on Twitter">Share on Twitter</TempButton>
  </React.StrictMode>,
  document.getElementById('root')
);
